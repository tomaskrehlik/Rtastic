      <h1>Build ggplot for rendering.</h1>

<div class="row">
  <div class="span8">
    <h2>Usage</h2>
    <pre><span class="functioncall"><a href='ggplot_build.html'>ggplot_build</a></span><span class="keyword">(</span><span class="symbol">plot</span><span class="keyword">)</span></pre>
    
    <h2>Arguments</h2>
    <dl>
      <dt>plot</dt>
      <dd>ggplot object</dd>
    </dl>
    
    <div class="Description">
      <h2>Description</h2>
      
      <p>This function takes the plot object, and performs all
  steps necessary to produce an object that can be
  rendered.  This function outputs two pieces: a list of
  data frames (one for each layer), and a panel object,
  which contain all information about axis limits, breaks
  etc.</p>
  
    </div>
      </div>
  <div class="span4">
    <!-- <ul>
      <li>ggplot_build</li>
    </ul>
    <ul>
      <li>internal</li>
    </ul> -->
      
    <h2>See also</h2>
    
  <code><a href='print.ggplot.html'>print.ggplot</a></code> and <code>link{benchplot}</code> for
  for functions that contain the complete set of steps for
  generating a ggplot2 plot.

        
  </div>
</div>