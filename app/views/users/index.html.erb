<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <span><i class="icon-user"></i> <%= link_to user.name, user %></span> | <span><i class="icon-envelope"></i> <%= user.email %></span>
    </li>
  <% end %>
</ul>